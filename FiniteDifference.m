clear, clcformat long% Define constantsalpha = 0.1;tmax = 20;% Define step sizesdeltax = 0.05;% For convergence and stability, deltat is defined based on deltaxdeltat = (deltax^2)/(4*alpha);% Define loop variablesi = 1; j = 1;% Create vectors for x and txvec = 0:deltax:1;tvec = 0:deltat:tmax;% Initialize T matrix_type% Each row of the T matrix represents the temperature values for the length of the rod at time step tT = zeros(length(tvec), length(xvec));T(:,1) = 0;T(1,2:length(xvec)) = 300; T(:,length(xvec)) = 300;for l=1:length(tvec)  t = tvec(l);    for i = 2:length(xvec)-1    x = xvec(i);    % Calculate coefficients to streamline code    coeff = (alpha * deltat)/(deltax^2);    % Call the heatsource function to determine the heat input from the sun    q = heatsource(x,t);    % Calculate Temperature at the ith spatial interval and the jth time step    T(l+1,i) = (deltat*q) + (coeff*T(l,i+1)) + ((1-(2*coeff))*T(l,i)) + (coeff*T(l,i+1));    endforendfor % Save key variables to workspace to simplify running multiple scriptssave TempMatrix.mat T;save xvec.mat xvec;save tvec.mat tvec;